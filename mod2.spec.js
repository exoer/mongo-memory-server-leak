
import ex from './module1'
describe('module', ()=>{
  it('cleans up', ()=> {
    let a = new Array(1e0)
    expect(a.length).toEqual(1e0)
    a = null
  })
  it('cleans up', ()=> {
    let a = new Array(1e0)
    expect(a.length).toEqual(1e0)
    a = null
  })
  it('cleans up', ()=> {
    let a = new Array(1e0)
    expect(a.length).toEqual(1e0)
    a = null
  })
  it('cleans up', ()=> {
    let a = new Array(1e0)
    expect(a.length).toEqual(1e0)
    a = null
  })
  it('cleans up', ()=> {
    let a = new Array(1e0)
    expect(a.length).toEqual(1e0)
    a = null
  })
})

